# Workflow derived from https://github.com/r-lib/actions/tree/v2/examples
# Need help debugging build failures? Start at https://github.com/r-lib/actions#where-to-find-help
#
# Bioconductor dependencies (SummarizedExperiment, SingleCellExperiment,
# TreeSummarizedExperiment, mia, miaViz, scater) need no extra step here:
# setup-r-dependencies resolves them with pak, which adds the Bioconductor
# repository matching the installed R version on its own. Do not add a
# BiocManager::install() step or an extra-repositories entry for them.
#
# setup-quarto is required by the tests in tests/testthat/test-fct_report.R;
# without it they skip instead of failing, which would hide a broken template.
on:
  push:
    branches: [main, dev]
  pull_request:

name: R-CMD-check.yaml

permissions: read-all

jobs:
  R-CMD-check:
    runs-on: ${{ matrix.config.os }}

    name: ${{ matrix.config.os }} (${{ matrix.config.r }})

    # windows-latest and ubuntu oldrel-1 were removed from this matrix. That is a
    # statement about CI coverage, not about support: neither platform is disowned,
    # they are simply not gated on here.
    #
    #   windows-latest (release) -- test-fct_export.R and test-fct_report.R both
    #     assert that a writer returns the path it was handed, and Windows
    #     normalizes it. A real portability defect in vpfkit, worth fixing on its
    #     own; until then it fails every run and says nothing about the change
    #     that triggered it.
    #   ubuntu-latest (oldrel-1) -- fails before reaching vpfkit at all: `mia`
    #     will not build against the current `rbiom` ("object 'unifrac' is not
    #     exported by 'namespace:rbiom'"). Upstream, and not ours to pin.
    #
    # A permanently red cell is worse than an absent one, because it trains
    # everyone to ignore the badge -- which is how three assertions that merely
    # described R's release wording survived here for as long as they did.
    strategy:
      fail-fast: false
      matrix:
        config:
          - {os: macos-latest,   r: 'release'}
          - {os: ubuntu-latest,  r: 'devel', http-user-agent: 'release'}
          - {os: ubuntu-latest,  r: 'release'}

    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      R_KEEP_PKG_SOURCE: yes

    steps:
      - uses: actions/checkout@v6

      - uses: r-lib/actions/setup-pandoc@v2

      - uses: quarto-dev/quarto-actions/setup@v2

      - uses: r-lib/actions/setup-r@v2
        with:
          r-version: ${{ matrix.config.r }}
          http-user-agent: ${{ matrix.config.http-user-agent }}
          use-public-rspm: true

      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: any::rcmdcheck
          needs: check

      - uses: r-lib/actions/check-r-package@v2
        with:
          upload-snapshots: true
          build_args: 'c("--no-manual","--compact-vignettes=gs+qpdf")'
